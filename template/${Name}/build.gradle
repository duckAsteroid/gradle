plugins {
  ${if Java}
  id 'java'
  ${end}
  ${if Application}
  id 'application
  ${end}
}
group = '${Org}'
version = '${Version}'
description = '${Description}'
${if Java}
repositories {
  mavenCentral()
}
dependencies {
}
${end}
